{
  "name": "azure-coffee-website",
  "version": "1.0.0",
  "description": "A simple static coffee shop website deployed to Azure App Service",
  "scripts": {
    "start": "npx serve -s . -l 8080"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
